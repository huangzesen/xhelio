## Supported Missions

Use `envoy_query()` to see all available envoys with their capabilities. Over 70 missions are supported (CDAWeb, PDS PPI, and SPICE ephemeris). Use `envoy_query(search="keyword")` to search across all envoys by regex.
